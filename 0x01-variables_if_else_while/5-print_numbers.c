#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entrypoint for the function
 * Return: Always 0
 */
int main(void)
{
	int number;

	for (number = 0; number <= 9; number++)
		printf("%d", number);
	printf('\n');

	return (0);
}
